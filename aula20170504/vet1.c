#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#define N 10000
int main () {
 strand(time(o));
 int i, maximo, media;
 int numeros[N];
 PRINTF("Entre com o valor natural maximo- ");
 scanf("%d", maximo);
 for (i=0; i< N; I++)
    numero[i]= rand()%maximo+1;
 media=0;
 for(i=0; i<N; i++)
    media += numeros[i];
 media/=N;
 printf("a media obtida eh &g./n", media);
 return 0;
 }
